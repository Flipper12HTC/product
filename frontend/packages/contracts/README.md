# @flipper/contracts

TypeScript types and validators shared across the Flipper 12 repos.

**This package is generated from `flipper12-backend/contracts/dist/ts/`. Do not edit by hand.**

The pinned version is in `VERSION`. When the backend bumps a contract version, pull the new files from the backend's `contracts/dist/ts/` into `src/` and update `VERSION`.
